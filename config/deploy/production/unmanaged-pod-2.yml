apiVersion: v1
kind: Pod
metadata:
  name: unmanaged-pod-2-1d10a504-80f1b5ff
  annotations:
    kubernetes-deploy.shopify.io/timeout-override: "60s"
  labels:
    type: unmanaged-pod
    name: unmanaged-pod-2-1d10a504-80f1b5ff
    app: hello-cloud
spec:
  activeDeadlineSeconds: 60
  restartPolicy: Never
  containers:
    - name: hello-cloud
      image: busybox
      imagePullPolicy: IfNotPresent
      command: ["sh", "-c", "echo 'Hello from the command runner!' && test 1 -eq 1"]
      env:
      - name: CONFIG
        valueFrom:
          configMapKeyRef:
            name: hello-cloud-configmap-data
            key: datapoint2
